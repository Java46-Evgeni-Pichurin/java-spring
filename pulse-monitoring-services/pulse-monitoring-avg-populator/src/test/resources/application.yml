logging:
  level:
    telran: trace
spring:
  cloud:
    config:
      enabled: false
  data:
    mongodb:
      database: test
      port: 27017
      host: localhost
de:
  flapdoodle:
    mongodb:
      embedded:
        version: 4.0.2